

export default class InfectSampleStorage {



    constructor() {

    }






    async storeSamples(samples) {
        const data = smaples.map(sample => sample.getRawData());

        // send to infect sample storage
    }
}
